credentials_corp:
  vault: "Corp"
  secrets:
    HF_TOKEN: "op://Corp/HuggingFace/credential"
    NGC_API_KEY: "op://Corp/NGC/credential"
    # Deliberately no GitHub token. GitHub auth is 'gh auth login' only; see the
    # GitHub auth policy in README.md. Do not re-add one here.
