credentials_corp:
  vault: "Corp"
  secrets:
    HF_TOKEN: "op://Corp/HuggingFace/credential"
    NGC_API_KEY: "op://Corp/NGC/credential"
    # GITHUB_TOKEN removed — use 'gh auth login' for filesystem-based auth
