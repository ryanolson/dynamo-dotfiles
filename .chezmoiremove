# Files to remove from target directory
# These files will be deleted when chezmoi applies

# For now, no files to remove by default
# Uncomment lines below as needed:

# Legacy Nix files (if migrating from Nix setup)
# .nix-*
# .config/nixpkgs/

# Old configuration files that are replaced
# .bashrc.backup*
# .zshrc.backup*
# .profile.backup*

# Broken symlinks
# .local/bin/broken-*