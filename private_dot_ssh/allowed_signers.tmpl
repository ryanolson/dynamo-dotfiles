{{- if and (hasKey . "onepassword") .onepassword.enabled .onepassword.ssh_agent .onepassword.signing_key_ref -}}
# Allowed SSH signers for git signature verification (managed by chezmoi)
{{ .email }} {{ onepasswordRead .onepassword.signing_key_ref | trim }}
{{- end }}
