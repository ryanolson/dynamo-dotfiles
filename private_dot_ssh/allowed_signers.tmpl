{{- if and (hasKey . "onepassword") .onepassword.enabled .onepassword.ssh_agent .onepassword.signing_public_key -}}
# Allowed SSH signers for git signature verification (managed by chezmoi)
{{ .email }} {{ .onepassword.signing_public_key | trim }}
{{- end }}
